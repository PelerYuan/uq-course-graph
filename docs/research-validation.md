# Validation notes

The UQ endpoint currently exposes both `/requirements/plan/{code}/{year}` and `/requirements/program/{code}/{year}` route templates. The scraper must support both. Live cross-faculty selector checks are pending.
