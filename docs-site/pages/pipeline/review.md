# Stage 3: review ambiguous rules

```r
source("03_review.R")
```

Review rows in a spreadsheet. Credit totals, grade thresholds, previous degrees, and prose conditions are not course edges. Keep them as special requirements unless the source text explicitly names a course relationship.

The desktop `edit()` helper is optional. CSV review is recommended because it works on servers and headless machines.
