# Docsify site

The `docs-site/` directory is the canonical user documentation. Preview locally with any static web server, for example:

```powershell
npx docsify-cli serve docs-site
```

The GitHub Pages workflow deploys this directory on pushes to `master`.
